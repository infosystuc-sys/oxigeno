# ─── Credenciales SQL Server ──────────────────────────────────────────────────
# Servidor: Infosys01 | Instancia: axsqlserver | BD: lg_distribuciones
# IMPORTANTE: No commitear este archivo. Está en .gitignore.

DB_USER=tu_usuario_sql
DB_PASSWORD=tu_contraseña_sql
DB_DATABASE=lg_distribuciones

# API REST (debe coincidir con VITE_API_URL del front)
API_PORT=3012

# Usuario para auditoría en NSFW_Destinos (login mock)
API_USUARIO=Sistema
